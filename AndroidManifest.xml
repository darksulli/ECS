<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.beta.ecs"
    android:versionCode="1"
    android:versionName="0.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.beta.ecs.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SecondActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SanitaireActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ElectriqueActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ChauffageActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".EcsActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SanitaireRobinetActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SanitaireRobinetRemplacerActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".SanitaireRobinetRemplacerCanalisationActivity" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ClientActivity" >
        </activity>
    </application>

</manifest>
